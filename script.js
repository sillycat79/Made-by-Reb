const filters=document.querySelectorAll('.filter'),samples=document.querySelectorAll('.sample');
    filters.forEach(button=>{const value=button.dataset.filter;if(value!=='all'&&![...samples].some(card=>card.dataset.category===value))button.hidden=true});
    filters.forEach(button=>button.addEventListener('click',()=>{filters.forEach(b=>b.classList.remove('active'));button.classList.add('active');const value=button.dataset.filter;samples.forEach(card=>card.hidden=value!=='all'&&card.dataset.category!==value)}));
    document.querySelectorAll('.sample').forEach((sample,i)=>{const video=sample.querySelector('.video-player'),sound=sample.querySelector('.sound-toggle');if(!video)return;const play=document.createElement('button');play.type='button';play.className='play-toggle';play.textContent='▶';play.setAttribute('aria-label','Play video '+(i+1));video.parentElement.appendChild(play);const toggle=()=>{if(video.paused){video.muted=false;video.play().catch(()=>{})}else video.pause()};play.addEventListener('click',toggle);video.addEventListener('click',toggle);video.addEventListener('play',()=>{sample.classList.add('playing');play.setAttribute('aria-label','Pause video '+(i+1))});video.addEventListener('pause',()=>{sample.classList.remove('playing');play.setAttribute('aria-label','Play video '+(i+1))});const syncSound=()=>{if(!sound)return;sound.textContent=video.muted?'🔇':'🔊';sound.setAttribute('aria-label',video.muted?'Unmute video':'Mute video')};video.addEventListener('volumechange',syncSound);syncSound();sound?.addEventListener('click',()=>{video.muted=!video.muted;if(video.paused)video.play().catch(()=>{})})});
    const uploadInputs=document.querySelectorAll('input[type="file"][data-video-index]');
    uploadInputs.forEach(input=>{input.addEventListener('change',event=>{const file=event.target.files?.[0];if(!file)return;const sample=input.closest('.sample');const video=sample?.querySelector('.video-player');if(!video)return;const url=URL.createObjectURL(file);video.src=url;video.muted=false;video.volume=1;video.load();video.play().catch(()=>{});});});
    let returnFocus; const modal=document.getElementById('modal'),title=document.getElementById('modalTitle'),close=document.querySelector('.modal-close');
    document.querySelectorAll('[data-modal]').forEach(button=>button.addEventListener('click',()=>{returnFocus=button;title.textContent=button.dataset.modal+' — coming soon';modal.classList.add('open');close.focus()}));
    function closeModal(){modal.classList.remove('open');returnFocus?.focus()};close.addEventListener('click',closeModal);modal.addEventListener('click',e=>{if(e.target===modal)closeModal()});document.addEventListener('keydown',e=>{if(e.key==='Escape')closeModal();if(e.key==='Tab'&&modal.classList.contains('open')){const nodes=modal.querySelectorAll('button,a');const first=nodes[0],last=nodes[nodes.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus()}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus()}}});
    document.getElementById('year').textContent=new Date().getFullYear();
